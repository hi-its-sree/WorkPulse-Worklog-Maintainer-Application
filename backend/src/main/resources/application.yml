spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workpulse
    username: workpulse
    password: workpulse123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: change-this-secret
  expiration-ms: 3600000
